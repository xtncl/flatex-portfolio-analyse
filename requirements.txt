# Abhängigkeiten  (Python 3.9+)
# Installation:  pip install -r requirements.txt
yfinance>=0.2.40
pandas>=1.5
matplotlib>=3.5
flask>=3.0

# Hinweise:
# - Internetzugang nötig (Yahoo Finance: Kurse, Devisen, Splits, ISIN-Suche).
# - Der interaktive HTML-Report lädt Plotly via CDN (kein pip-Paket nötig).
# - Kein LLM / kein API-Key erforderlich.

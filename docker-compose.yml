services:
  portfolio:
    image: ghcr.io/xtncl/flatex-portfolio-analyse:latest
    ports:
      - "8080:8080"
    volumes:
      - portfolio_data:/app/data
    restart: unless-stopped

volumes:
  portfolio_data:
